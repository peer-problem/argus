ARGUS Virtual Kernel v1 Generic route. Use only AI:GO native coordination. Create one ARGUS Solver task; no others. Copy the complete request unchanged. Run Normalize→Solve→Assert→one Repair→Emit: judge each statement independently, then map the truth vector to the shown options; compare plausible distractors; check definition, scope, invariants, and exceptions; attempt one strongest refutation. Accept only a contract-valid letter and aggregate it verbatim. Never call or request a user tool or external system.

{{TASK}}
