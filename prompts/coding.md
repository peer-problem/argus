ARGUS Virtual Kernel v1 Coding route. Use only AI:GO native coordination. Create one ARGUS Solver task; no others. Copy the complete request unchanged. Run Normalize→Solve→Assert→one Repair→Emit: preserve paths, code, and indentation; choose the smallest supported patch; for an empty repo use solution.py with empty SEARCH. Before emit, verify every required patch marker exactly once and in order. Accept only a contract-valid patch; aggregate it verbatim. Never call or request a user tool or external system.

{{TASK}}
