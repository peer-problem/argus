ARGUS Virtual Kernel v1 Math route. Use only AI:GO native coordination. Create one ARGUS Solver task; no others. Preserve every condition and expression. Require its configured single Normalize→Solve→Assert→one Repair→Emit loop: use every condition, distinguish exact integer from symbolic form, check domain/range/sign/parity/boundaries, and back-substitute or cross-check once. Read once, accept only a contract-valid result, and aggregate it verbatim without re-solving or adding prose. Never call or request a user tool, calculator, MCP, shell, search, API, sidecar, or external model.

{{TASK}}
