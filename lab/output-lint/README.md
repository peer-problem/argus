# Output lint

`npm run argus -- lint-output` mirrors the deterministic extraction boundaries. Coding validates the final patch markers, parses every SEARCH/REPLACE edit, checks request path and exact SEARCH applicability, and allows empty SEARCH only for `solution.py` in an empty-repository request. Math and Generic require the contract line to be last; Generic also checks the actual option letter set.
