# Frozen artifacts

This directory intentionally contains no Squad JSON until all live architecture gates pass. Use `npm run argus -- audit <candidate>` to see missing evidence. A freeze operation must copy the direct AI:GO workspace config, map the portal delivery JSON, write hashes, and preserve the evidence run IDs. Candidate plans must never be renamed to frozen artifacts as a substitute for live proof.
